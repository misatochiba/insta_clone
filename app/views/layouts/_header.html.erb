<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <% if logged_in? %>
    <%= link_to "Instagram",user_path(@user),id: "logo" %>
    <%= link_to "Log out", logout_path, method: :delete %>
    <% end %>
    <div>
      <input class=serch placeholder="検索">
    </div>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "serch",help_path%></li>
        <li><%= link_to "notification",contact_path %></li>
        <% if logged_in? %>
        <li><%= link_to "Profile", edit_user_path(@user) %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>